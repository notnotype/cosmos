# Cosmos 原始需求记录

> 本文件是 append-only 的用户原文真相源。只允许按时间追加新的用户原文或明确勘误；不修辞、不总结、不把架构解释写回原文。原文中的重复、错别字和未确定表达均保留。

## 2026-08-06：项目起点

> 我要开发一个新项目。先读这个文件。ssh arch
> /home/notnotype/CodeRepository/daily-digest-research/RESEARCH.md
>
> **---**
>
> 我打算新建一个项目（叫做 cosmos），工作流和 AGENTS.md 可以迁移过去
>
> 先来进行架构设计。
>
> ---
>
> 很像是生产者消费者模型。

## 2026-08-06：第一轮需求和能力

> 接下来你作为一位专业的架构师进行架构设计，架构要灵活可扩展。
> 接下我来提出需求和能力。你来来你进行架构设计和调整。接下来我要提出第一次的需求和能力了。先建立 cosmos 仓库。设计好了后把架构输出到一个文件中，我的原始需求也记录到一个文件中（防止失真），后续几轮可以在此文件基础上调整。
>
> 1. 关于 Source，是否类似 github 的 action，既能用户手动触发，也能定时触发，也能自定义触发器（例如轮询检测邮箱，如果有新邮件则触发）。关于触发后应该执行的代码也是可以自定义的，甚至可以跑 agent。
> 2. RawObservation.url 这个字段，你需要考虑类似：消息来源于 Telegram、公众号、群聊这种。没有 url 的。
> 3. 系统应该是要维护一个信息库，这个信息库的原则是尽可能收集更多用户关注领域的信息。也就是说这个项目是信息聚合平台，用户不用亲自去花费时间去各个平台浏览。项目先去各个渠道收集信息（这可以使用各个平台的推荐系统 + 搜索系统），对应于用户自己操作的随意浏览阶段（项目相比用户自己来做，它能浏览更多信息，深度也更广，自定义程度也更高）。这是第一阶段，原始信息录入（把多个渠道的消息聚合，也就是信息录入后，本地项目离线也能访问已经录入的信息。需要存图片，这里我可以放宽为：“尽可能”）。信息库需要设计得非常灵活便于查询，分类、便签、BM5 这方面你可以考虑一下。
> 4. 经过第一阶段，信息库中存的一般都是与用户设定的注意比较相关的信息。对于这些信息，他们可能是这样的：
>    - 用户 BiliBili、X 等平台账号的主页推荐（可能每个小时就录入一批）
>    - 用户 BiliBili、X 等平台账号的关注用户（频率可能高一点）
>    - 某个网站的公告（靠触发器轮询）
>    - AIHOT 网站（去要去重）
>    - 邮件消息
> 5. 接下来就是考虑如何把这些信息展示给用户了。我觉得这个应用把消息展示给用户有两种：第一种是看板，另一种是推送。
>    - 看板需要展示：当前热点（热点可以是系统设置的，例如：DeepSeek API 状态追踪，或者是用户设置的，例如：XX 大会），精华消息（系统Agent精选过的信息，cosmos agent 通常会对这些主体进行深入。并且可以生成可视化的页面。例如：某一篇技术博客：LLM 先读一遍然后可以给出批注和观点。例如Agent 在信息库中发现了某一个竞品项目，自主进行深入调研产出报告。）。其他信息流（推荐系统，这一部分通常不用 LLM 参与。）总之看板界面的自定义程度很高
>    - 推送：考虑 Telegram、邮箱场景。目前先专注看板把。这个推送通常用于紧急消息、热点的推送
> 6. 目前系统中存在的实体：原始信息，话题（包含多个原始信息），精华（包含多个原始信息和多个话题，同时还带文件、附件、和可视化页面（数据形态可以给一个文件夹））（热点应该算精华的一种）（我觉得这些名字可能有点不太恰当）
>
> ---
>
> 我期待的使用场景，举例：
>
> 1. 早上八点向 QQ、Telegram、Email 等渠道推送一版摘要（形态可以是渲染出来的图片，并附上一份网页链接，点击直接进入软件的看板界面）
> 2. 点开看板（下面这两个属于热点主体，肯定有同类的信源。不同的信肯定能让我们更全面的看待一件事物。点进去还可以看到相关联的信息，例如：《本地部署 Qwen-Image-3.0-Pro 教程》，属于推荐系统的部分了）：
>    - 字节 Seed 发布 SeedRealtime 音视频全双工大模型字节 Seed 发布 SeedRealtime 音视频全双工大模型
>    - 阿里通义千问发布 Qwen-Image-3.0-Pro 与 Standard阿里通义千问发布 Qwen-Image-3.0-Pro 与 Standard
> 3. 热点下面就是精华：是 Agent 为用户精选的板块。例如：每天记五个单词（这个板块每天都可以推荐，用户还能交互式地完成这个任务），例如深入了解 claude 原理-part5（agent 把学习任务拆分到每一天。用户可以渐进式的学习）。今日关于 AI 写作类项目竞品分析（每天都可以更新一次）。（这个精华能力被我扩展了）
> 4. 精华下面就是消息流，可以用户分类、分区（娱乐，硬件、开发等）浏览。走推荐系统

## 2026-08-06：仓库位置修正

> 在本机 C:\Users\notnotype\Documents\CodeRepository\GithubProjects 建立。

## 2026-08-06：文档补充

> 记得整理一份完整的项目需求文档和项目介绍

## 2026-08-06：领域建模与上下文维护

> 现在来建模，搞清楚并定义或者重新调整这些  Source、Trigger、Flow、Action 等概念。维护 CONTEXT.md
>
> 先给我列出目前存在哪些概念、实体需要对其

## 2026-08-06：CONTEXT.md 范围校准

> 我先要提一点就是目前的 CONTEXT.md 有点过度设计了。这些概念应该是产品级或者更上一级的概念，并且要记录的是经常用的、有歧义的地方，这些都是开发的时候长出来的，你目前列的有点细了，可以考虑精简一下。可以参考一下 NeuroBook 的 CONTEXT.md
>
> 你应该拿出来和我讨论的是例如：话题、分类、精华、热点这种核心的东西

## 2026-08-06：信息层次、相关推荐与 Feature 模型校准

> 这里可能需要用到一些推荐算法或者其他领域的知识，如果有用到你要适时教一下我。
>
> 我的设计，可能推翻之前的一些设定，你可以先评估名字的合理性：
>
> 1. 原始信息/信息条目/原始报导，举例：
>    - BiliBili：qwen3.8max发布
>    - AIHot：qwen3.8 max 发布
>    - X：qwen3.8 max 发布
>    - Jeff Dean 等四位 AI 元老创立 Discovery Loop
>    - DeepSeek 涨价
>    - 某个 BiliBili 的娱乐视频
>
> 2. 信息：对信息条目聚类，例如：qwen3.8max发布，Jeff Dean 等四位 AI 元老创立 Discovery Loop。（这里需要拆开来讨论一下。我想知道推荐系统是如何推荐相关内容的？例如：Jeff Dean 等四位 AI 元老创立 Discovery Loop这条消息和Jeff Dean 退出 gemini 有关。）
>
> 以上这两层是基础。下面的概念我觉得都是对消息的聚合 + 加工
>
> 话题/Story/timeline/热点。我觉得这个应该算是一个可以长期跟进的东西。类似知乎的热榜。例如：
>
> - 如何看待 DeepSeek 8 月 6 日公告称即将大幅度涨价？
> - 为什么 Jeff Dean 从 Google 离职引起这么大的轰动？
> - 怎么看 OpenAI 工程师说「现在大多数大型实验室成员几乎不看论文，因为大部分都是欺诈」？
> - 台风登陆
>
> 我觉得这些都可以统一成 Feature 或者 Artifact。要不要做成话题是比较主观的。例如 Agent 发现了大量 DeepSeek 涨价的消息，于是创建一个话题，话题持续追踪相关的信息。例如 Agent 发现台风登陆这个也适合做成一个话题。
>
> 对于 Feature 来说。它应该持有很多与它话题相关的消息。并且这个消息能持续更新。Feature 由一个 Agent 来维护。并且提供集中模板（timeline，topic，custom）例如：台风就是和做成 timeline。为什么 Jeff Dean 从 Google 离职引起这么大的轰动？ 就适合做成 topic。
>
> 每天背单词的需求也可以用 Feature 来做。
>
> 还有精华其实也可以用 Feature 来实现。
>
> （这个 Feature 模型有点混乱，可能设计得有点不好。理一下）

## 2026-08-07：领域名称、Story、Topic 与推荐基线确认

> 可以，下一轮待确认：
> 1. 接受 Subject -> Topic
> 2. 接受Feature -> Workspace，栏目或者专题空间
> 3. Story 默认由每个事件型 Entry 自动创建，允许一个 Story 一个 Entry 这样的存在。上级用的是 Story，不直接用 Entry
> 4. Agent 自动创建 Topic 时。默认激活
> 5. Topic 不收录 Entry，收录 Story
> 6. 先不上 embedding
> 7. 自动 Topic 和 Spotlight 的过期、合并与每日维护预算可配置，Agent 可维护
>
> ---
>
> 然后 [$grilling](C:\\Users\\notnotype\\.agents\\skills\\grilling\\SKILL.md) 一下

## 2026-08-07：Grilling Round 1 回答

> 1. 同意
> 2. 同意
> 3. 关于这个问题。active、visible、splotlighted、subscribed 这些字段是否把 Topic 和系统的其他部分耦合过多了呢？
> 4. 我觉得可以做成永不过期，因为定义一个话题过期不准确，可能某些话题跨数年。最多是人类主动归档。不过这个过期、归档优先级不高，可以以后再做
> 5. 我觉得人类和 Agent 都能相互一起协作，就和多人协作一样。能看到操作者、修改者
> 6. 同意
> 7. 同意
> 8. 同意

## 2026-08-07：Grilling Round 2 回答

> 9. 同意，但是这四种核心 kind 不是最终版，我建议例如 media 可以细分，例如漫画，动漫等。
> 10. 同意
> 11. 同意
> 12. 同意自动过期
> 13. 同意，但是第一版不用考虑这么多权限相关的问题
> 14. 同意
> 15. 同意，同时预算系统也不是最需要讨论的部分，第一版保持简单即可

## 2026-08-07：Grilling Round 3 回答

> 16. 同意
> 17. 同意
> 18. 同意
> 19. 同意

## 2026-08-07：Grilling Round 4 回答与 Workspace 状态补充

> 全部同意。同时提醒一下：Workspace 的状态这一块你需要考虑一下（workspace 可能有更新状态，就是 agent 在更新）

## 2026-08-07：Grilling Round 5 回答

> 全部同意

## 2026-08-07：Grilling Round 6 回答

> 34. 同意
> 35. 同意
> 36. 权限不是第一版需要考虑的问题
> 37. 同意

## 2026-08-07：React 技术栈、Agent Harness 与部署模式初步决策

> 我想走 react 生态。用 next tailwind [**shadcn**](https://claudeskills.info/skills/shadcn/ui/shadcn/) **Prisma sqlite docker React Hook Form+Zod这一套。**
>
> 同时你可以去调研一下目前最流行，LLM，或者你最熟悉的技术栈是什么？

> 1. 开发使用 bun，但是生产用 node
> 2. LLM UI/调用：使用 pi-ai。或者C:\Users\notnotype\Documents\CodeRepository\GithubProjects\neuro-agent-harness。和这个 agent 一起开发（这里面的 tsx profile 和 session 模型已经抽象好了）
> 3. [https://github.com/shadcn-ui/ui/tree/HEAD/skills/shadcn](https://github.com/shadcn-ui/ui/tree/HEAD/skills/shadcn) 这里提供 skill，可以按照 shadcn 的指导来看看怎么用
> 4. 【决策点 2】同意
> 5. 注意本次只是初步决策，后续如果发现不好用或者有更好的选择可以改，例如 sqlite 这边
> 6. 还有桌面壳：产品应该支持：服务器部署模式（主要先做这个，但是做的时候要考虑尽量为以后的扩展兼容）、客户端模式、客户端与服务分离模式

> 关于 neuro-agent-harness 的设计理念（记录一下）：
>
> - TSX Profile 可以去领域化包含在 neuro-agent-harness 里，它的上下文管理很好用
> - sidecar 功能取消掉，移出 neuro-agent-harness。这个可以靠 workflow 来代替
> - 去领域化，附带常用工具（read 这些）。附带 SSE
> - 需要提醒的是 NeuroBook 的 Harness 又演进了许多，修复了许多 bug。所以 neuro-agent-harness 可能有点落后
> - 目前阶段用到 agent 的地方比较少，可以先直接用 pi-ai。等 neuro-agent-harness 稳定后再切
>
> ---
>
> shadcn 可以装，装到通用 skill 目录 codex 目录 claude 目录内

## 2026-08-08：从用户角度审查数据库、生产者、适配器、Pipeline、LLM 与推荐

> Run Step Job Domain DomainEvent 是什么？
>
> ---
>
> 现在这个架构是否完整。我们接下来从用户的角度来审查一下。
>
> 应用最核心的地方是数据库。生产者是能扩展的
>
> 目前应用应该是要扩展性特别强的，例如 opencli BiliBili 插件（或者叫做适配器）应该有这些功能。
>
> 1. 登陆功能：凭证存哪里？由适配器自己存？或者 cosmos 提供统一的自定义状态管理？
> 2. 用户配置信息抓取策略。这里的模型应该是怎样的？和之前 Worker 的关系是什么？例如用户配置每三十分钟抓取一次动态，每两个小时抓取一次推荐流。这里插件或者叫做适配器应该怎么和 Worker 配合？
> 3. 然后是如何把抓到的 Entry 变成 Story？这个功能是什么模型做的？我觉得既要有机器算法分类，也要有 LLM 协助分类。考虑这些情况：入站的时候发现（数据pipeline？可以自定义代码或者 LLM？这里以 LLM 为例）某个用户特别关注的领域，LLM 可以派发 job 去深入调研获取更多数据，或者发现例如 DeepSeek API 宕机这种紧急的信息，LLM 设置可以去多个平台搜素。总之这个地方要设计的非常灵活开放
> 4. 第三点引入了一个 pipeline 的东西，这个东西我觉得有点意思，不知道目前架构有没有类似的概念。同时第三点也引入了一个角色：LLM 充当 “知识管理员”，变成了中央统筹大脑（以前这里可以用代码和算法做，但是有 LLM 则更加灵活）。LLM 和代码方案结合
> 5. 推荐系统怎么做？代码和 LLM 结合。

## 2026-08-08：知识管理者、nb-memory 与偏好模型修正

> 这一版设计得清晰多了。
>
> 这个偏好的地方和我想的有歧义。
>
> 1. Agent 记忆系统可以用 C:\Users\notnotype\Documents\CodeRepository\GithubProjects\nb-memory，你可以去调研一下。可以同步开发
> 2. 应该是 Agent + Cosmos 自己观察到的行为 + 以后可能还有别的信息 -> 程序可读的配置。你提到的 “每个程序配置都应记录” 目前有设计过度了
> 3. 平台自身的推荐信号这个我只是提一下，先不进行建模考虑
> 4. Agent Session 不是全部记忆这一节可以删了。先不展开
>
> 关于知识管理者（这里只是草案，后续随时可以调整）：
>
> 1. 这是用户与系统交互的窗口，他的权限很大，并且能代替用户的 GUI。
> 2. 用户与系统交互的接口：web gui（web 里可以直接和知识管理者聊天）、知识管理者（通过 cosmos cli 实现），cosmos cli
> 3. 知识管理者不是一个 session，他能有很多分身，但是共享一个大小记忆、知识库（nb-memory 维护）。例如和用户聊天的 session。ingest 过程也可以参与（可以实现知识点细究等操作）
>
> ---
>
> 决策点1：同意
> 决策点2：同意
> 决策点3：参考知识管理者草案，不深究
> 决策点4：目前阶段不考虑
>
> 上一轮讨论另一个 agent 已经同步了。这一轮的可以同步到文档中了。

## 2026-08-08：Workflow 核心、脚本优先与 Ingest/Research 解耦

> 1. 我觉得脚本模式才是 workflow 最底层最灵活的形态，IR 和 comfy 这类graph模式的都可以转换成脚本（temporal）模式，nb-workflow 底层就是基于脚本模式的
>
> 关于Ingest，确实混合了许多东西。
>
> 1. 外部来源事实不需要等 LLM 后才入库
> 2. 同意，旧 Observation 永远不覆盖
> 3. Entry 如何变成 Story。我觉得这里也比较适合做成自定义流程（用户 和  Agent 都可以配置）。例如：
>    - 策略1：所有 Entry 都分批走 Agent 过一遍
>    - 策略2：先用脚本策略过一遍，难以决策的，强相关的，重要的过 Agent
> 4. 研究任务如何派发（我觉得这里不要和 ingest 耦合）：研究任务我觉得也可以做成 workflow。ingest 标记为紧急。Trigger 触发，workflow 开始执行研究任务。（这个架构如何，还有更好的吗？）

## 2026-08-08：Workflow 收敛为核心运行时

> 所以说，目前很多东西都可以收敛到 workflow 了。workflow 变成了核心，所以在实施的时候要注意 workflow api 的灵活。
>
> 1. 信息入库（workflow 编排）
> 2. 研究型任务：既要访问信息库又要主动访问渠道信息
> 3. ingest 也是一种 workflow
> 4. 要不要为 workflow 分类？

## 2026-08-11：服务拆分、nb-workflow、队列和 API 合同

> 1. 关于现在的服务架构：Web、NestJS API、Worker 能否部署在不同的宿主上？目前 NestJS 和 Worker 是绑死的吗？二者通过 sqlite 交流？以后可以考虑拆分。Worker 也可以暴露 API 服务。目前 sqlite 承担了消息队列的职责吗？以后是否能把 redis 加入这个架构？
> 2. 目前这个 spike 和 nb-workflow 的区别是什么？好像计划是扩展 nb-workflow 的。Worker 基于 nb-workflow 构建

> 我觉得 nb-workflow 可以像 langchain 一样提供，持久化是和选的。和你说的一样拆成多个组件。
> ——-
> 好，那你现在来重新设计一下整体的架构

> 关于：“Worker 使用数据库轮询，不需要 Redis。”。这里要不要引入一个队列？用 redis 这种队列是不是更好？Worker 允许多并发吗？并发控制好不好处理？
> 关于 wf.invoke_agent：目前这个好像是依赖 neuro-agent-harness 的。等 harness 文档后可以接入。

> 同意。
> 可以把新架构同步一下。nb-workflow 这边架构只是初步的草案，实际执行应该新开任务继续调整。

> 我觉得下一步就是可以定义好 NestJS 以及 Worker 这两个向外提供的 API。

> 1. 同意
> 2. 同意
> ---
> 下一步深入调查分析，了解原始需求和系统所需要的能力。然后制定完整的 API、DTO 草案（后续实现过程中可修改）。单独存放。最后派发多个子代理进行审查，查漏补缺，看是否还有什么未考虑到的情况。

## 2026-08-11：文档收口与 `nb-workflow` 优先顺序

> 等一下，本轮还是先同步文档。整理项目。把之前对话中定好的 API 草案、架构同步到文档中。其他的地方还是先不要动。
>
> 整理完成停止。后续后可以考虑规划先做 nb-workflow，等 nb-workflow 稳定后。参考 spike 出来的 workflow runtime 和本次定好的 Worker API 草案来实现 Worker。

## 2026-08-20：Cosmos 前端组件实验室与 UI 路线

> 关于 cosmos 前端。能否做一个组件实验室用于调节组件？
>
> ---
>
> 按照项目最新的开发流程，如果要从零开始实现这个需求的前端部分应该怎么样开始？
> ---
> 1. C:\Users\notnotype\Documents\CodeRepository\GithubProjects\neuro-book\packages\nb-ui 这里有一个 vue 版本的 ui 库。他能实现组件级别自定义。要不要也做一个 react 版本的？同时把 NeuroBook + macOS 系列配色作为默认主题？
>
> ---
>
> 那现在的 cosmos 能否先作为消费者使用 shadcn 提供的默认样式？后续再换呢？
>
> ---
>
> 可以搭建 ui 组件调试，实验室。后续所有的组件都需要能在这里调。
